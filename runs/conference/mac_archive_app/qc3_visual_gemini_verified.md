# Gemini 3.5 Flash 视觉 QC-3（已验证图像输入）

日期：2026-07-11  
提供方：aishuo  
请求模型/返回模型：`gemini-3.5-flash` / `gemini-3.5-flash`  
输入：`design/assets/adaptive-finder-media-visual-target.png` 原始 1487 × 1058 PNG，通过 OpenAI-compatible `image_url` data URL 直接发送。

## 输入真实性探针

模型准确识别：左上角红/黄/绿窗口按钮；标题`品牌素材与文档.zip`；中央雪山、森林、湖泊倒影。这与 Codex 原图检查一致。此前 Hermes `--image` 会话把图误认成 LobeChat，判定为图像路由失败，本文件取代其视觉结论。

## 判定

`NO-GO`，方向正确但视觉目标图必须修订后才可作为实现基准。

### P0/P1 汇总

1. 左侧同时出现系统侧栏与独立归档目录栏，形成双侧栏；最小窗口会四栏挤压。改为单一归档侧栏 + 主内容 + 可折叠检查器。
2. `更多文件…`像网页分页，不符合 Finder 的滚动/虚拟化列表，删除。
3. `附近的项目`中文不自然且 Finder Gallery 条带无需标题，删除标题。
4. 顶部`打开/创建归档/解压缩`与当前文档语境、右侧`解压缩所选项目`重复。文档窗口只保留添加、解压缩、检测、视图、分享/更多和搜索；检查器不放重复主操作。
5. 左侧`操作记录`按今天/昨天/本周/月占据导航层，背离 Finder 熟悉感。当前任务放工具栏弹出层，历史放独立窗口。
6. 左二栏底部 `+ / − / ⋯`像侧栏编辑器，不像归档内容导航，删除。

## 可保留

- 右侧信息检查器字段和密度接近原生。
- 中央大预览 + 底部媒体条带符合用户批准的媒体模式。
- 状态栏与选中态清楚。
- 玻璃材质主要在导航/控件层，内容没有被玻璃覆盖。

## LOOP 记录

- 观察：图像真实性通过；结构性问题集中在双侧栏、重复操作、非原生分页与中文。
- 决策：不推翻 Finder + 媒体方向，只重绘成单侧栏稳定外壳。
- 下一步：生成修订视觉目标，再由 Codex 原图检查；Gemini 仅作第二读者。

## 路由证据

响应返回模型为 `gemini-3.5-flash`，`finish_reason=stop`。代理 usage 报告未单独计量 image tokens，故视觉真实性以识别探针和实际内容一致性证明，不以该计量字段证明。
