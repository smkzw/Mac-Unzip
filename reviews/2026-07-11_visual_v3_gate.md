# 归档工作台视觉目标 v3 门禁

日期：2026-07-11  
目标图：`design/assets/adaptive-finder-media-visual-target-v3.png`  
SHA-256：`e7b660774274b7549895d502776bc9b5b863c8bb8a7e55ddc3549c6f8feee7cb`

## 路由与真实性

aishuo/Gemini-3.5-Flash 通过 data URL 直接读取原始 PNG，准确复述标题`品牌素材与文档.zip`、84 项、单一左侧归档导航、中央画廊/媒体条带、右侧检查器和雪山湖泊主图。请求/返回模型均为 `gemini-3.5-flash`。输出因 token 长度在结尾截断，但 GO/问题清单已完整返回。

## Codex 原始分辨率判定

**GO，作为实现视觉目标。**

- v1 的双侧栏、网页式`更多文件…`、日期历史树、`附近的项目`和重复解压入口均已消失。
- v2 的内联缩放工具条和检查器内部 `<<` 已移除；信息切换位于标准工具栏。
- 目录计数 48 + 12 + 15 + 9 = 84，与标题一致。
- Liquid Glass/系统材质只在工具栏、侧栏、检查器和控件层；内容保持清晰。
- 最小窗口由规格的 900 × 560 pt、检查器优先折叠和系统 toolbar overflow 处理，不能从静态图证明，留给运行时验收。

## 对 Gemini 意见的处置

- “选中行贴边”：原图存在明显左右 inset，观察与像素不符，拒绝。
- “计数应使用 badge”：与其 v2 的“去掉胶囊背景”意见冲突，也不适用于当前文件夹计数语境；保留淡化尾随文本。
- 状态栏文案：采纳语义方向，运行时 String Catalog 使用`已选择 1 个项目，8.4 MB`/复数模板，不把静态图的中点格式当成最终字符串。
- 检查器路径：实现使用系统路径组件/可访问性分隔，不把带空格的斜杠硬编码为最终组件。

## 实现对照合同

真实应用第一次出现文档态 UI 后，必须以相同 1487 × 1058 viewport 截图，与本图并排输入视觉比较；浅色、深色、降低透明度、增强对比度和 900 × 560 最小窗口分别验收。本图是结构/层级目标，不是伪造资产或可交互性证明。
