你是 Codex 主会场邀请的中文产品体验复核员，模型路由固定为 aishuo / MiniMax-M3。

请先完整读取并遵守 `/Users/smkzw/.hermes/SOUL.md`。

Hard boundaries:
- 除下列唯一允许文件外，禁止调用工具读取其他文件。
- 禁止浏览、禁止运行命令或测试、禁止修改代码。
- 下面的“固定证据包”是唯一产品证据来源。
- 只评审中文术语、用户工作流、命令层级和能力表达；不得声称已做渲染视觉验收。
- Write exactly one output file: `runs/conference/zip_provider_vertical_slice/aishuo_minimax_bounded_rerun.md`。不得创建其他文件。

Read these files only:
- `prompts/conference/zip_provider_vertical_slice/aishuo_minimax_bounded_rerun.md`

目标：判断真实 ZIP 垂直切片的中文界面是否诚实、Finder 化、且“检测完整性”是否保持为低频二级功能。

固定证据包（Codex 从当前源码逐项核对后提供）：

1. 工具栏：左侧标题为实际 ZIP 文件名，第二行显示当前目录和搜索条件下的 `visibleEntries.count`。标题固定单行、中间截断，无圆角标题背景。工具栏包含返回、前进、添加、解压缩、列表视图、媒体预览、信息、`操作`、原生 NSSearchField。添加和解压缩在真实 provider 状态下禁用，并分别提示“添加功能将在安全编辑事务接入后启用”“完整解压工作流将在目标目录安全校验接入后启用”。

2. `操作` 菜单：共享、标签、检测完整性、分隔线、当前操作。`检测完整性` 不在主工具栏；真实 provider 和 UI fixture 均设置 `canTestIntegrity = false`，所以不可操作。当前没有任何“已排队”的可达假状态。

3. 侧边栏：不再使用四个静态假文件夹，而是从 ZIP 条目生成顶层文件夹及实际文件数；选择文件夹会筛选列表。根目录文件归入“压缩包根目录”。

4. 列表：Finder 式四列“名称 / 类型 / 大小 / 修改日期”；名称列只显示最后一级文件名，完整路径仍用于元数据和搜索；单行、中间截断、悬停显示全文。

5. 错误文案已逐类区分：需要密码、密码错误、不支持的加密方式、不支持的压缩方式、缺少分卷、不安全路径、资源超限、读取期间源文件变化、读取失败、不支持的格式、压缩包损坏。

6. 搜索：原生 NSSearchField，宽屏占位“搜索压缩包内容”，窄屏“搜索”；搜索结果和侧边栏目录共同决定 `visibleEntries`，标题计数随之变化。

7. 当前限制：添加、完整解压、编辑事务、完整性检测真实工作流尚未接入，因此相关动作保持禁用；本轮没有当前 provider 状态的渲染截图，不能做视觉验收。

请输出严格六节，控制在 900 字以内：

# MiniMax-M3 中文体验复核
## 边界
## 已通过
## 仍需修改
## 证据与推断
## 验证缺口
## 建议下一步

要求：
- 明确回答“检测完整性放二级菜单是否合适”。
- 只根据证据包判断，不补充未提供的源码事实。
- 区分事实、推断和建议。
- 若没有阻断项，直接写“当前证据包内无中文体验阻断项”。
